## Mini-CRM: Сервис распределения лидов

Mini-CRM для автоматического распределения входящих обращений (лидов) по операторам с учетом их нагрузки и заданных "компетенций" (весов) для каждого источника.

### 🛠️ Технологический стек

| Категория         | Технология         |
| :---------------- | :----------------- |
| **Язык**          | Python             |
| **Web-фреймворк** | FastAPI            |
| **ORM**           | SQLAlchemy (Async) |
| **База данных**   | SQLite             |

---

## ⚙️ 1. Запуск проекта

### Требования

-   git
-   docker

### Установка и запуск

1.  Клонируйте репозиторий:

    ```bash
    git clone [https://github.com/666waffle666/Mini-CRM.git](https://github.com/666waffle666/Mini-CRM.git)
    cd Mini-CRM
    ```

2.  Соберите и запустите Docker контейнер:

    ```bash
    docker build -t mini_crm .
    docker run -p 8000:8000 mini_crm
    ```

    _Проект будет доступен по адресу: `http://127.0.0.1:8000`_

---

## 📜 2. Модель данных

Модель данных реализует необходимые концепции: Оператор, Лид, Источник и Обращение.

### Ключевые сущности (Таблицы)

| Сущность                 | Описание                                                               | Основные поля, определяющие логику                                                 |
| :----------------------- | :--------------------------------------------------------------------- | :--------------------------------------------------------------------------------- |
| **`Operator`**           | Сотрудник, принимающий обращения.                                      | `is_active` (активен/не активен), `max_load` (лимит по активным лидам/обращениям). |
| **`Lead`**               | Конечный клиент.                                                       | `external_id` (Уникальный внешний идентификатор, email, телефон и т.п.).           |
| **`Source`**             | Канал обращения (бот).                                                 |                                                                                    |
| **`Contact`**            | Конкретное обращение Лида из определенного Источника.                  | `lead_id`, `source_id`, `operator_id` (назначенный оператор).                      |
| **`DistributionConfig`** | Хранит числовой вес (компетенцию) Оператора для конкретного Источника. | `operator_id`, `source_id`, `weight`.                                              |

### Взаимосвязи

-   **`DistributionConfig`** связывает `Operator` и `Source` по принципу Many-to-Many (М:М) и хранит `weight`.
-   **`Contact`** связан с `Lead`, `Source` и назначенным `Operator` по принципу Many-to-One (М:1).

---

## 🧠 3. Логика распределения обращений

Процесс регистрации нового обращения (`POST /contacts/`) автоматически распределяет лида.

### А.Определение принадлежности Лида

-   **Как определяется один Лид:** Система использует внешний идентификатор (`external_id`) или другие уникальные данные из входного запроса.
-   **Логика:** Если Лид найден, обращение связывается с ним, если нет — создается новый Лид.

### Б.Учет лимитов нагрузки

-   **Нагрузка:** Нагрузкой оператора считается **текущее количество активных обращений** (`Contact`), которые ему назначены.
    -   _Обоснование_: Выбрано отслеживание количества **обращений**, а не лидов, для более точного контроля ресурсов.Активное обращение — это обращение со статусом `is_active = True`.
-   **Доступность:** Оператор включается в список доступных, только если он **активен** и его текущая нагрузка **не превышает** лимит (`max_load`).

### В.Распределение с учетом весов

-   **Алгоритм распределения:** Используется **случайный выбор с вероятностью** (Weighted Random Choice).
    1. Находятся все **доступные** операторы, назначенные на данный `Source`.
    2. Для каждого подходящего оператора берется его `weight` из `DistributionConfig`.
    3. Оператор выбирается случайным образом с вероятностью: $$\frac{weight_i}{\sum (weights \text{ доступных операторов})}$$
-   **Обработка исчерпанного лимита:** Если выбранный оператор "выиграл", но его лимит уже исчерпан, он **исключается**, и система **повторяет выбор** среди оставшихся подходящих операторов.

### Г.Если подходящих операторов нет

-   **Выбранный вариант:** Обращение **создается без назначенного оператора**.
    -   В этом случае `operator_id` для `Contact` остается пустым (`NULL`).

---

## 🗃️ 4. API (Набор операций)

Доступ к API через `/docs` (Swagger UI).

### 4.1.Управление операторами

-   `POST /operators/` - Создать оператора.
-   `GET /operators/` - Просмотр списка операторов.
-   `PATCH /operators/{id}` - Управление лимитом нагрузки (`max_load`) и активностью (`is_active`).

### 4.2.Настройка распределения по источникам

-   `POST /sources/` - Создание источника (бота).
-   `POST /sources/{source_id}/operators` - Установить веса (`weights`).

### 4.3.Регистрация обращения

-   `POST /contacts/` - Регистрация нового обращения.
-   `GET /contacts/` - Просмотр списка обращений.

### 4.4.Просмотр состояния

-   `GET /leads/` - Просмотр списка лидов.
-   `GET /stats/operators` - Просмотр распределения обращений по операторам и источникам.
